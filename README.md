# Selfmade Portfolio — React (Vite)

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run development server

```bash
npm run dev
```

3. Build for production

```bash
npm run build
npm run preview
```

Notes:

- This project uses Vite + React. Ensure Node.js v16+ is installed.
- The app entry is `src/main.jsx` and main component is `src/App.jsx`.
